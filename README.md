# elements-shuffle-sort

## It has two approaches. One is through React and the other one is JavaScript.

For React project please do a npm install first and then run npm start.

JavaScript can be run directly.